import React from "react";
import { render } from "@testing-library/react";
import { DropdownProps, BasicDropDown } from "./index";

/*
it('should render with the correct text', () => {
  const { getByText } = render(<BasicDropDown label='hello from Button' />);
  const rendered = getByText('hello from Button');
  expect(rendered).toBeTruthy();
});

it('calling click event handler correctly', () => {
  function onButtonClick() {
    alert('Clicked and working fine');
  }

  const { getByText } = render(<Button label={"test"} onClick={onButtonClick} />);
  const rendered = 1; //getByText('Clicked and working fine');
  expect(rendered).toBeTruthy();
});
*/
